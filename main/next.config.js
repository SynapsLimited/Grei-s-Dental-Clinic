/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['en', 'al', 'it'], // Include all your locales
    defaultLocale: 'en',
  },
  // ...other configurations
};

module.exports = nextConfig;
